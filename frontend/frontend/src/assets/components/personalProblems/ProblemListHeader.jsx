
const ProblemListHeader = ({ additionalStyles }) => {
  return (
    <h2 className={`text-nsw text-2xl font-medium -mt-6 mb-4 ${additionalStyles}`}>
      Lista personală de probleme
    </h2>
  );
};

export default ProblemListHeader;
